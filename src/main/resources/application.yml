spring:
  application:
    name: mall-product
  cloud:
    nacos:
      server-addr: nacos-service.infrastructure.svc.cluster.local
      username: ${NACOS_USERNAME}
      password: ${NACOS_PASSWORD}
      config:
        enabled: true
        namespace: ${NACOS_NAMESPACE}
        group: BASE
        refresh-enabled: true
      discovery:
        enabled: true
        register-enabled: true
        namespace: ${NACOS_NAMESPACE}
        group: BASE

  config:
    import:
      - nacos:mall-product-config.yaml

management:
  endpoint:
    health:
      show-details: never
  endpoints:
    web:
      exposure:
        include: health,info,metrics